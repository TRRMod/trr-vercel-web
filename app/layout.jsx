import './globals.css';

export const metadata = { title: 'TRR Keyboard Build', description: 'TRR Keyboard Build Showcase' };

export default function RootLayout({ children }) {
  return <html lang="th"><body>{children}</body></html>;
}
